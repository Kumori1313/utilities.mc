[package]
name = "app"
version = "0.1.0"
edition = "2024"

# Per Part 6: cdylib for the wasm-bindgen output, rlib so the crate stays
# usable as a normal Rust dependency (and natively testable).
[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2.126"
